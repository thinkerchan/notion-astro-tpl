---
const { tag } = Astro.props;
---

<span
  class="bg-gray-200 text-gray-600 px-1.5 py-0.5 rounded text-xs"
  style={`background-color: color-mix(in srgb, ${tag.color} 30%, transparent); box-shadow: 1px 1px 2px rgba(0, 0, 0, 0.1);`}
>
  {tag.name}
</span>