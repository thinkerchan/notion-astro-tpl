---
const { tag } = Astro.props;
---

<span
  class="inline-block px-1.5 py-0.5 sm:px-2 sm:py-1 text-xs font-medium border border-neutral-200 rounded-md transition-colors duration-200 hover:opacity-80"
  style={`
    background-color: color-mix(in srgb, ${tag.color} 15%, white);
    color: color-mix(in srgb, ${tag.color} 50%, #1e293b);
  `}
>
  {tag.name}
</span>